#include <iostream>
#include <dlfcn.h>
//#include "steamSearch.h"

using namespace std;

int main()
{
	// void *handle;
	// void (*test)(void);
	// char *error;
	// handle= dlopen("steamSearchlib.so", RTLD_LAZY);
	// if (!handle)
	// {
	// 	fprintf(stderr, "%s\n", dlerror());
	// 	exit(1);
	// }
	// dlerror();
	// steamSearch s;
	// s.test();
	// dlclose(handle);
	cout << "here";
}